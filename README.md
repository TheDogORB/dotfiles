Just a bunch of . files of my Arch install(s)

Includes settings for following pacman packages:
```ark btop dolphin easyeffects gamemode kitty mangohud nvim openrazer-daemon openrgb plasma-meta```

Includes settings for following AUR packages:
```fastfetch```

RazerBatteryTray is expected to be present at `/opt/RazerBatteryTray/`
- `./icons` - generated icons via `./src/generate_new_bat_icons.py` available at original repo/my custom python file
- `./src`
